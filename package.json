{
  "name": "luxus-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "LUXUS PERÚ — frontend (Astro + React islands). Consume la API de luxus-api.",
  "engines": { "node": ">=20.11" },
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "npm run build -w @luxus/shared && npm run dev -w @luxus/web",
    "dev:shared": "npm run dev -w @luxus/shared",
    "build": "npm run build -w @luxus/shared && npm run build -w @luxus/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
